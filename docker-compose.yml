version: '3'
services:
  # ...
  enjambre:
    # ...
    environment:
      # ...
      - SWARM_CODEX_CREDS=/creds/codex/auth.json
      - SWARM_CODEX_BIN=codex
    volumes:
      # ...
      - ${SWARM_CODEX_CREDS}:/tmp/codex/auth.json:ro
      # ...