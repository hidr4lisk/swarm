# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Django
db.sqlite3
staticfiles/

# Bundle portátil (lo arma scripts/build_bundle.sh; no va al repo)
dist/

# Entorno local — acá pueden vivir rutas personales; jamás al repo
.env
PLAN.md

# Credenciales — ningún archivo local de creds entra al repo
*.credentials.json
*credentials*
!seed-credentials.sh

# Bóveda de API keys (ruta portable): cifrada, pero JAMÁS al repo (menos aún público)
data/secrets.enc
data/.secrets.runtime.json
data/.ratelimit.json
data/.ratelimit.tmp
data/.ratelimit.lock/
secrets.enc
.secrets.runtime.json

# Editor / SO
.idea/
.vscode/
*.swp
.DS_Store
